# HarmonyOS 命令行构建指导

## 环境配置

### 1. 工具路径配置
- **命令行工具路径**: `/Users/liujiarui/Downloads/command-line-tools`
- **SDK路径**: `/Users/liujiarui/Library/OpenHarmony/Sdk`
- **SDK版本**: 20版本

### 2. 环境变量配置

已配置的环境变量：
```bash
export PATH="/Users/liujiarui/Downloads/command-line-tools/bin:$PATH"
```

### 3. 工具版本验证

当前已安装的工具版本：
- **codelinter**: 6.0.140 (代码检查工具)
- **hvigorw**: 6.0.6 (构建工具)
- **hstack**: 5.1.0 (堆栈解析工具)
- **ohpm**: 5.3.2 (依赖管理工具)

## 常用构建命令

### 基础命令

#### 查询命令
```bash
# 查看hvigorw帮助信息
hvigorw -h
hvigorw --help

# 查看hvigorw版本信息
hvigorw -v
hvigorw --version
hvigorw version
```

#### 清理构建
```bash
# 清理构建产物build目录
hvigorw clean
```

### 构建命令

#### HAP应用构建
```bash
# 构建HAP应用（默认debug模式）
hvigorw assembleHap

# 指定debug模式构建
hvigorw assembleHap -p buildMode=debug

# 指定release模式构建
hvigorw assembleHap -p buildMode=release
```

#### APP应用构建
```bash
# 构建APP应用（默认release模式）
hvigorw assembleApp

# 指定构建模式
hvigorw assembleApp -p buildMode=debug
hvigorw assembleApp -p buildMode=release
```

#### HSP包构建
```bash
# 构建HSP包
hvigorw assembleHsp

# 指定构建模式
hvigorw assembleHsp -p buildMode=debug
hvigorw assembleHsp -p buildMode=release
```

#### HAR包构建
```bash
# 构建HAR包
hvigorw assembleHar

# 指定构建模式
hvigorw assembleHar -p buildMode=debug
hvigorw assembleHar -p buildMode=release
```

### 高级构建选项

#### 指定产品构建
```bash
# 指定product进行编译
hvigorw assembleHap -p product=ProductName
```

#### 指定模块构建
```bash
# 指定模块及target进行编译（需要与--mode module参数搭配使用）
hvigorw assembleHap -p module=ModuleName@TargetName --mode module

# 编译多个模块（用逗号分割）
hvigorw assembleHap -p module=Module1@default,Module2@default --mode module
```

#### 调试选项
```bash
# 覆盖debuggable配置
hvigorw assembleHap -p debuggable=true
hvigorw assembleHap -p debuggable=false

# 执行测试框架代码覆盖率插桩编译
hvigorw assembleHap -p ohos-test-coverage=true
hvigorw assembleHap -p coverage=true
```

#### 参数配置文件
```bash
# 使用参数配置文件
hvigorw assembleHap -p parameterFile=param.json5
```

### 测试相关命令

#### 设备测试
```bash
# 执行Instrument Test
hvigorw onDeviceTest -p module=moduleName

# 带覆盖率报告的测试
hvigorw onDeviceTest -p module=moduleName -p coverage=true

# 指定测试范围
hvigorw onDeviceTest -p module=moduleName -p scope=suiteName#methodName

# 启用ASan检测（hvigorw 5.19.0+）
hvigorw onDeviceTest -p module=moduleName -p ohos-debug-asan=true

# 多模块测试（用逗号分割）
hvigorw onDeviceTest -p module=module1,module2 -p coverage=true
```

#### 本地测试
```bash
# 执行Local Test（不支持Linux）
hvigorw test -p module=moduleName

# 带覆盖率的本地测试
hvigorw test -p module=moduleName -p coverage=true

# 指定测试范围
hvigorw test -p module=moduleName -p scope=suiteName#methodName
```

### 覆盖率报告

#### 生成覆盖率报告
```bash
# 基于打点数据生成覆盖率统计报表
hvigorw collectCoverage
```

#### 报告路径
- **覆盖率测试报告**: `<module-path>/.test/default/outputs/ohosTest/reports`
- **测试结果文件**: `path_to_project/module_name/.test/default/intermediates/ohosTest/coverage_data/test_result.txt`
- **ASan日志路径**: `<module-path>/.test/default/intermediates/ohosTest/coverage_data`

## 代码检查

### 使用codelinter进行代码检查
```bash
# 检查当前项目
codelinter

# 查看codelinter版本
codelinter -v

# 查看codelinter帮助
codelinter -h
```

## 依赖管理

### 使用ohpm管理依赖
```bash
# 安装依赖
ohpm install

# 查看已安装的包
ohpm list

# 查看ohpm版本
ohpm -v

# 查看ohpm帮助
ohpm -h
```

## 堆栈解析

### 使用hstack进行堆栈解析
```bash
# 查看hstack版本
hstack -v

# 查看hstack帮助
hstack -h
```

## 项目构建流程

### 标准构建流程
1. **清理项目**
   ```bash
   hvigorw clean
   ```

2. **安装依赖**
   ```bash
   ohpm install
   ```

3. **代码检查**
   ```bash
   codelinter
   ```

4. **构建应用**
   ```bash
   # 开发版本
   hvigorw assembleHap -p buildMode=debug
   
   # 发布版本
   hvigorw assembleApp -p buildMode=release
   ```

5. **运行测试**
   ```bash
   hvigorw test -p coverage=true
   ```

### 快速构建命令
```bash
# 一键清理并构建HAP（debug模式）
hvigorw clean && hvigorw assembleHap -p buildMode=debug

# 一键清理并构建APP（release模式）
hvigorw clean && hvigorw assembleApp -p buildMode=release
```

## 注意事项

1. **命令执行位置**: 大部分hvigorw命令需要在工程根目录下执行
2. **版本兼容性**: 从hvigorw 5.18.4版本开始，版本查询命令支持在任意路径下执行
3. **构建模式**: 
   - 构建Hap/Hsp/Har时默认为debug模式
   - 构建App时默认为release模式
4. **测试限制**: Local Test暂不支持在Linux上执行
5. **ASan检测**: 需要hvigorw 5.19.0+版本支持

## 故障排除

### 常见问题
1. **命令未找到**: 检查环境变量PATH配置是否正确
2. **构建失败**: 检查项目配置文件和依赖是否正确
3. **测试失败**: 确保设备连接正常，模块配置正确

### 环境验证
```bash
# 验证所有工具是否正常
codelinter -v && hvigorw -v && hstack -v && ohpm -v
```

如果所有工具都能正常显示版本号，说明环境配置成功。